[tool.black]
line-length = 100
target-version = ['py37', 'py38', 'py39']
skip-string-normalization = true
requires = [
    "gitpython",
    "setuptools",
    "wheel",
]
build-backend = "setuptools.build_meta"
